# Required: routing decisions (Groq LLM call)
GROQ_API_KEY=

# Required only for media preprocessing (image OCR/caption + voice-note ASR).
# Not needed if cache/media_analysis.json is already fully populated.
GEMINI_API_KEY=
